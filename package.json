{
  "name": "tiber-strategy",
  "version": "1.0.0",
  "private": true,
  "description": "TIBER strategy ontology artifact producer. Ships deterministic strategy vocabulary contracts (rules, not per-player labels) for TIBER consumers.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build-dynasty-strategy-ontology.js",
    "validate": "node scripts/validate-dynasty-strategy-ontology.js",
    "test": "node --test \"tests/**/*.test.js\""
  }
}
