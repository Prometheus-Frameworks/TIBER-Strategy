/**
 * Artifact envelope metadata for DYNASTY_STRATEGY_ONTOLOGY_V1.
 *
 * GENERATED_AT is a fixed constant, not a wall-clock read. The ontology is
 * hand-authored contract content, so the build must be byte-deterministic:
 * the same source always produces the same artifact. Bump this timestamp
 * (and MODEL_VERSION) when the ontology content changes.
 */
export const ARTIFACT_TYPE = 'DYNASTY_STRATEGY_ONTOLOGY_V1';
export const SCHEMA_VERSION = 'dynasty_strategy_ontology_v1';
export const MODEL_VERSION = 'dynasty-strategy-ontology-v1.0.0';
export const GENERATED_AT = '2026-06-10T00:00:00.000Z';

/**
 * row_count is always 0 for this artifact family. The ontology contains
 * concept rows, not player rows; row_count counts player rows by TIBER
 * convention and this artifact must never contain any.
 */
export const ROW_COUNT = 0;
